context EObject{
	
	constraint size {		
		check : self->size() = 1 
	}
	
	constraint type {
		guard: self.satisfies("size")		
		check : self.isKindOf(Edge)
	}
	
	constraint shape {
		guard: self.satisfies("type")
		check: self.type = EdgeViewType#dashline
	}

}