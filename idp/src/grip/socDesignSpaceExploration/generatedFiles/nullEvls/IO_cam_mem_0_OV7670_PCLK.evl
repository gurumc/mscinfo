context EObject{
	
	constraint size {		
		check : self->size() = 1 
	}
	
	constraint type {
		guard: self.satisfies("size")		
		check : ExternalPortReferenceType.all.size() = 1		
	}

}

context ExternalPortReferenceType{

	constraint name {
		check: self.portRef = "cam_mem_0_OV7670_PCLK"
	}
}